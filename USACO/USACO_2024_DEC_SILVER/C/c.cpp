#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    //freopen("input.in", "r", stdin);
}

//ideas
//start with all as usable cells
//add in each conveyor belt
//check if next cell in path is unusable
    //if it is unusable, then this cell is also unusable
    //