#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int h, w;
vector<string> grid;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    freopen("input.in", "r", stdin);

    cin >> h >> w;
    grid = vector<string>(h);
    for(int i=0; i<h; i++){
        cin >> grid[i];
    }

    
}