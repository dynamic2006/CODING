#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    freopen("input.in", "r", stdin);

    int n; cin >> n;
    vector<int> trains(n);
    for(int i=0; i<n; i++){
        cin >> trains[i];
        
    }
}

//IDEAS
//simulate the explosions
//