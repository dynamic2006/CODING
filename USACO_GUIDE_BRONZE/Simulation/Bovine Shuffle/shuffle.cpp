#include <iostream>
#include <vector>
using namespace std;

int code[100]; int cowline[100];

void find_next_pos(int cow_id, int cur_pos)
{
    
}

int main()
{

    freopen("shuffle.in" , "r" , stdin);
    freopen("shuffle.out" , "w" , stdout);
    
    int N, temp; cin >> N;
    for(int i = 0; i<N; i++){
        cin >> temp;
        code[i] = temp;
    }
    for(int i = 0; i<N; i++){
        cin >> temp;
        cowline[i] = temp;
    }

    for(int i=0; i<3; i++)
    {
        
    }
}